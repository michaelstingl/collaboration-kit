<!-- PR body. One physical line per paragraph or bullet. Delete these comments before posting. Title goes in `gh pr create --title`. -->
<!-- Cross-repo kit: cross-reference the sibling PRs here (e.g. "part of <slug>; depends on server#14"). -->

Closes #<n>

## What

<The change, in one or two lines.>

## Why

<Root cause, and why this is the right fix — the alternative you rejected, if any.>

## Verification

<!-- The gate is the one check independent of reasoning. Show before/after. -->

- `<typecheck>` → <result>
- `<test>` → <result>
- `<build>` → <result>

<Name any untested gap. Confirm the change respects your project's dominant constraint.>
